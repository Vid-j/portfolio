# Email capture now writes to data/subscribers.csv via /api/subscribe.
# No Formspree ID required for local development.

# Optional: asset base path (defaults to / on Vercel, /portfolio/ elsewhere)
# VITE_BASE_PATH=/
